## 2026-05-14

### Summary
- Updated the canonical docs to match the new `env_diy` architecture boundary.
- Documented pure map JSON, task-agnostic base `info`, and wrapper-injected
  reward/task logic.
- Added a new worklog entry for the boundary refactor and documentation sync.

### Files Changed
- `docs/README.md`
- `docs/guides/env-overview.md`
- `docs/guides/training.md`
- `docs/reference/env-api.md`
- `docs/reference/rewards.md`
- `docs/reference/tasks-and-validators.md`
- `docs/reference/benchmark-v0.md`
- `docs/project/development-guide.md`
- `docs/worklog/2026-05-14.md`

### Behavior Changes
- Documentation now treats map JSON as pure world-construction data instead of
  a task/reward carrier.
- Base `info` is now documented as generic game state plus generic events only.
- Reward is now documented as an external wrapper concern instead of a base env
  `info["reward"]` contract.
- Benchmark docs now describe task wiring through `TaskSpec` and
  `RewardWrapper`, not `reward_mode`.

### Tests
- Command: `./.venv/bin/pytest tests/test_env_diy_architecture_boundaries.py -q`
- Result: pass
- Command: `./.venv/bin/python -m env_diy.scripts.smoke_test_env`
- Result: pass
- Command: `./.venv/bin/python rl/train_random.py --episodes 1 --max-steps 20 --seed 0`
- Result: pass
- Command: `./.venv/bin/python rl/train_single_task.py --task avoid_traps --episodes 1 --max-steps 20 --seed 0`
- Result: pass
- Command: `./.venv/bin/python -m env_diy.benchmark.eval --suite NesyLink-v0 --policy random --episodes 1 --seed 0`
- Result: pass

### Known Issues / Next Steps
- The DreamerV3 adapter documentation still needs a dedicated follow-up refresh
  if task-shaped wrappers are added to that integration path.
- Historical worklog entries intentionally still describe the earlier
  task-in-map and reward-in-info design where that was true at the time.

## RL Training Guide

### Summary
- Added a dedicated RL training guide under `docs/guides/`.
- Documented how to use base observations and base `info` under the new
  architecture boundary.
- Documented wrapper-based task reward design and current Stable-Baselines3
  training usage.

### Files Changed
- `docs/guides/rl-training.md`
- `docs/README.md`
- `docs/worklog/2026-05-14.md`

### Behavior Changes
- No runtime behavior changed.
- The docs now have a dedicated canonical guide for RL training that is
  separate from the DreamerV3 guide.

### Tests
- Command: `rg -n "info\\[\\\"task\\\"\\]|info\\[\\\"reward\\\"\\]|RewardWrapper|Stable-Baselines3|MultiInputPolicy" docs/guides/rl-training.md docs/README.md`
- Result: pass

### Known Issues / Next Steps
- The new guide documents the current SB3 and wrapper path only. A future task
  can add examples for custom feature extractors, vector normalization, and
  curriculum scheduling if those become stable repository patterns.
