# Worklog 2026-05-05

## Task: Fix Monster Collision Bug, Add Monster HP/Kill System, Add Victory Condition

### Changes Made

#### 1. Fix Shield Knockback Bug (`env_diy/envs/dungeon_env.py`)
- Removed `0.0` distance from knockback loop (it always "succeeded" without moving the monster)
- Added `_apply_player_separation()` fallback: when monster knockback fails (stuck against wall/edge), the player is pushed away from the monster to break overlap

#### 2. Monster HP & Kill System
- **`env_diy/core/constants.py`**: Added `MONSTER_DEFAULT_HP = 3`, `MONSTER_KILL_GOLD_REWARD = 2`
- **`env_diy/entities/monsters.py`**: Added `max_hp` field to `MonsterState`, default HP now uses `MONSTER_DEFAULT_HP`
- **`env_diy/envs/dungeon_env.py`**: Modified `_resolve_monster_contact()`:
  - Contact without shield now reduces monster HP by 1
  - Monster killed when HP <= 0: removed from room, player awarded gold
  - Added `monsters_hp` to observation space and `_get_obs()`

#### 3. Victory Condition (`env_diy/envs/dungeon_env.py`)
- Added `_all_chests_opened()` check: terminates episode with "VICTORY" when all chests opened
- Added `info["victory"]` flag in `_get_info()`
- Victory awards +1.0 reward bonus

#### 4. Game Display (`env_diy/app/game.py`)
- Added game-over and victory overlay messages
- Any key press restarts after game over or victory

### Test Results
- All 53 existing tests pass
- Manual smoke test passed

---

## Follow-up Fixes

### Fix 1: Victory Condition Now Checks All Rooms
- `_all_chests_opened()` previously only checked current room's chests
- Now iterates over ALL room templates via `room_manager.room_templates`, builds/gets each room state, checks every chest
- Victory only triggers when ALL chests across ALL 4 rooms are opened

### Fix 2: Shield Block Also Damages Monster
- `_resolve_monster_contact()` shield branch now reduces `monster.hp -= 1`
- If monster HP reaches 0 from shield bash, monster is killed and gold awarded
- Removed early `return 0.0` — uses `break` so monster removal cleanup executes
- Display shows remaining HP: "SHIELD BLOCK (1HP LEFT)" or "SHIELD KILL +2G"
